// Configurações Globais do EpiConecta
window.EPI_CONFIG = {
  // URL do backend em produção.
  // Substitua pela URL fornecida pela plataforma de hospedagem (ex: Render, Railway, Fly.io)
  // Exemplo: 'https://epi-backend.onrender.com/api/v1'
  API_URL: 'https://epi-backend.onrender.com/api/v1'
};
